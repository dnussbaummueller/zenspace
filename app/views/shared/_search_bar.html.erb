<%= form_with url: yoga_classes_path, method: :get, class: "d-flex w-50 position-relative" do %>
  <%= text_field_tag :query,
    params[:query],
    class: "form-control rounded",
    placeholder: "Search for a yoga class"
  %>
  <button name="button" type="submit" class="btn position-absolute" style="right: 3px; top: 4px;">
    <i class="fas fa-search d-flex justify-content-center"></i>
  </button>
<% end %>
