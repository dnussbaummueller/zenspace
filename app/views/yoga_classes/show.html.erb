
<div class="container">
  <%# Photo Carousel: %>
  <%= cl_image_tag @yoga_class.photos.first.key, class: "d-block w-100" %>
  <div class="overlay d-block w-100 d-flex justify-content-center align-items-center">
    <h1 class="text-white"><%= @yoga_class.name %></h1>
  </div>

  <%# Booking Card: %>
  <div class="booking-card my-card d-flex justify-content-between mt-3">
    <h2 class="booking-price"><strong><%= @yoga_class.price %>€</strong></h2>
    <%= link_to "Book Now", yoga_class_path(@yoga_class), class: "btn-flat btn-booking w-25" %>
  </div>


    <div class="show-container d-flex justify-content-between mt-3 flex-grow-1">
      <%# Info Card: %>
      <div class="info-card my-card border-bottom flex-grow-1">
        <div class="title-bar d-flex justify-content-between align-items-start border-bottom mb-3 pb-2">
          <h1><%= @yoga_class.name %></h1>
          <%# render 'partials/average_rating', yoga_studio: @yoga_studio %>
        </div>
        <div class="short-details border-bottom pb-2">
          <p><i class="fa-solid fa-location-dot"></i> <%= @yoga_class.capacity %></p>
          <p><i class="fa-solid fa-tag"></i> <%= @yoga_class.price %>€</p>
        </div>
        <p class="info-text mt-3"><%= @yoga_class.description %></p>
      </div>

    <%# Teacher Card: %>
    <div class="teacher-card my-card flex-grow-1">
      <div class="teacher-card-header d-flex justify-content-between align-items-center border-bottom pb-3 mb-3">
        <div class="d-flex flex-column">
          <h2>Teacher</h2>
          <%= render 'shared/average_rating', reviewable_type: @yoga_class.yoga_studio_teacher.teacher %>
        </div>

        <div class="teacher-avatar">
          <%= cl_image_tag @yoga_class.yoga_studio_teacher.teacher.photos.first.key, class: "avatar" %>
        </div>
      </div>
      <div class="teacher-card-body">
        <h3><%= @yoga_class.yoga_studio_teacher.teacher.name %></h3>
        <p><%= @yoga_class.yoga_studio_teacher.teacher.description %></p>
      </div>
    </div>

    <%# Studio Card %>
    <div class="studio-card my-card flex-grow-1">
      <div class="studio-card-header d-flex justify-content-between align-items-center border-bottom pb-3 mb-3">
        <div class="d-flex flex-column">
          <h2>Studio</h2>
          <%= render 'shared/average_rating', reviewable_type: @yoga_class.yoga_studio_teacher.yoga_studio %>
        </div>
        <div class="studio-avatar">
          <%= cl_image_tag @yoga_class.yoga_studio_teacher.yoga_studio.photos.first.key, class: "avatar" %>
        </div>
      </div>
      <div class="studio-card-body">
        <h3><%= @yoga_class.yoga_studio_teacher.yoga_studio.name %></h3>
        <p><%= @yoga_class.yoga_studio_teacher.yoga_studio.description %></p>
      </div>
    </div>



</div>
